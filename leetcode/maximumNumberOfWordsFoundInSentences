class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        wordCnt = []
        for i in sentences:
            wordCnt.append(len(i.split()))
            
        return max(wordCnt)
